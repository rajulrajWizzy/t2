{
    "version": 2,
    "buildCommand": "npm install && NODE_OPTIONS='--no-warnings' npm run vercel-build",
    "installCommand": "npm install",
    "framework": "nextjs",
    "environmentVariables": {
      "NEXT_TELEMETRY_DISABLED": "1",
      "NODE_ENV": "production",
      "SKIP_INSTALL_DEPS": "1"
    }
  }