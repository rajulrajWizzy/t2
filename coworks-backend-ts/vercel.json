{
    "version": 2,
    "builds": [
      {
        "src": "package.json",
        "use": "@vercel/next",
        "config": {
          "installCommand": "npm install typescript && npm install"
        }
      }
    ],
    "env": {
      "NODE_ENV": "production"
    }
}